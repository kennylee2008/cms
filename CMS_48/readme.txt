提供如下功能的参考实现：

在查看文章的时候，需更新文章的点击量
	- 为了更准确记录文章的点击量，特制定如下规则：同一个IP在一个小时之内多次访问同一篇文章，
	只算一次访问
	- 即在更新文章点击量记录，在一个小时之内同一个IP的连续点击，将不重复计算点击量！
	- 为了加快访问速度，利用ServletContext作为缓存
		* 每个ip在访问某篇文章的时候，在ServletContext中记录其访问时间
		* 在决定是否记录访问量的时候，判断其上一次访问的时间是否在一个小时之内